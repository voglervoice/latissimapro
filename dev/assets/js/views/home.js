define([
    "jquery",
    "tweenmax",
    "events",
    "publisher",
    "raphaeljs"
], function($, TweenMax, Events, publisher, raphael) {
    /**
     * MainMenu
     * @param {HTMLElement} target
     * @constructor
     */
    var Home = function(target) {
        var self = this;

        
    };

    return Home;
});