define([
    "jquery",
    "tweenmax",
    "events",
    "publisher",
    "raphaeljs"
], function($, TweenMax, Events, publisher, Raphael) {

    var Design = function(target) {
        var self = this;
      

        // ******************* public ******************* 
        this.elem = $('#design');
        this.resize = function(w, h){
            
        };

        this.initOpen = function(){

        };

        this.open = function(){
        
        };

        this.close = function(){
        };

        // ******************* private *******************
         var init = function(index){
        };

        init();
    };

    return Design;
});