define([
    "jquery",
    "tweenmax",
    "events",
    "publisher",
    "raphaeljs"
], function($, TweenMax, Events, publisher, Raphael) {

    var MilkSystem = function(target) {
        var self = this;
      

        // ******************* public ******************* 
        this.elem = $('#milksystem');
        this.resize = function(w, h){
            
        };

        this.initOpen = function(){

        };

        this.open = function(){
        
        };

        this.close = function(){
        };

        // ******************* private *******************
         var init = function(index){
        };

        init();
    };

    return MilkSystem;
});